function toggleProgress(event, element) {
    const progressBar = element.querySelector('.progres');
    const progress = parseInt(progressBar.getAttribute('data-progres'));
    progressBar.style.width = progress + '%';
}

function resetProgress(element) {
    const progressBar = element.querySelector('.progres');
    progressBar.style.width = '0';
}
